# 2026-04-22 工作日志

## 每日推送 PDF 化优化（续）
- 确认两个自动化任务（automation-2 考公日报、ai-2 AI资讯日报）的 prompt 已包含完整 PDF 生成指令
- 两个任务均配置了：读取 gen_daily_report.py → Python 调用 build_xxx_report() → 生成 PDF 到 output/ → deliver_attachments 发送
- AI资讯日报额外增加了图片下载逻辑（curl 下载配图到本地再传入 images 参数）
- 在 output/ 目录创建了 .gitkeep 确保 Git 追踪
- gen_daily_report.py 已支持 build_exam_report() 和 build_ai_report() 两个快捷函数，均接受 images 参数

## 周计划 App 知识库更新提醒功能
- 在知识库首页每个分类卡片上新增"上次更新：X天前"视觉提醒
- 用 localStorage (schedule_notes_last_update) 记录每个分类最后添加自定义知识点的时间戳
- 颜色分级：今天更新=绿色，3天内=绿色，7天内=橙色，超过7天=红色+提示"建议更新"
- 未添加过自定义内容的分类显示红色"暂未添加自定义内容"
- 每次通过模态框保存自定义知识点时自动调用 touchNotesUpdate(cat) 刷新时间戳
- 对应新增 CSS：.notes-update-hint / .urgent / .warn / .ok
- 对应新增 JS：loadNotesLastUpdate / saveNotesLastUpdate / touchNotesUpdate / notesUpdateHint
